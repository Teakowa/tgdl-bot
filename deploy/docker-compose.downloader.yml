services:
  downloader:
    image: ghcr.io/teakowa/tgdl-downloader:${DOWNLOADER_IMAGE_TAG:-latest}
    env_file:
      - ../.env
    environment:
      TDL_BIN: /usr/local/bin/tdl
    volumes:
      - tgdl-tdl-session:/root/.tdl
    restart: unless-stopped

volumes:
  tgdl-tdl-session:
    name: tgdl-tdl-session
